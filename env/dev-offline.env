# Dev run with no backend: swaps in the dev-only fakes under lib/data/dev.
# Never use these flags in a release build (the app refuses to start).
API_BASE_URL=https://api.dev.aeropass.example
SENTRY_DSN=https://8eab04c9f4d0568602e177f176a5c8c0@o4512131329359872.ingest.us.sentry.io/4512133554438144
SENTRY_ENVIRONMENT=dev
SENTRY_SEND_TEST_EVENT=false
USE_FAKE_CONSENT_BACKEND=true
USE_FAKE_VERIFICATION_BACKEND=true
# 011: set to true only after the Sentry alert rule on failure_class:service exists.
SENTRY_ALERT_RULE_CONFIRMED=false
# 011 device walk-through only: uncomment to force a failure (service_failure or hang).
# DEV_VERIFICATION_FAILURE=service_failure
